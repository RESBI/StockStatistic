# P7 — 高级 Handlers（Tier 2~6）实现规划

> **Phase**：P7
> **目标**：实现 31 个高级 task_type（统计/信号/非线性/灰色/ML）
> **前置依赖**：P3（Compute 核心）
> **预计耗时**：2.5 周
> **测试数**：200

---

## 1. 目标与范围

实现 PAXG v1~v7 所需的**全部高级计算能力**：

| Tier | 类别 | task_type 数 | PAXG 对应 |
|------|------|-------------|----------|
| 2 | 经典统计检验 | 8 | v1~v6 |
| 3 | 信号处理与频域 | 5 | v7 W/E |
| 4 | 非线性动力学 | 7 | v7 N |
| 5 | 灰色系统 | 3 | v7 G |
| 6 | 机器学习 | 7 | v7 F + 通用 |
| **合计** | | **30** | |

---

## 2. 前置依赖

- **P3 Compute**：Handler 注册表、TaskExecutor、dispatch 函数

---

## 3. 实现任务清单

### 3.1 Tier 2 — 经典统计检验（8 个）

| ID | task_type | 文件 | PAXG 对应 | 优先级 |
|----|-----------|------|----------|--------|
| P7-01 | `correlation` | `handlers/stats/correlation.py` | v1/v4/v7 | P0 |
| P7-02 | `hypothesis_test` | `handlers/stats/hypothesis.py` | v3/v6 | P0 |
| P7-03 | `bootstrap` | `handlers/stats/bootstrap.py` | v4 | P0 |
| P7-04 | `permutation_test` | `handlers/stats/permutation.py` | v4/v7 | P0 |
| P7-05 | `chow_test` | `handlers/stats/chow.py` | v4 | P1 |
| P7-06 | `survival_analysis` | `handlers/stats/survival.py` | v6 | P1 |
| P7-07 | `ecdf` | `handlers/stats/ecdf.py` | v6 | P1 |
| P7-08 | `multiple_testing` | `handlers/stats/multiple_testing.py` | v4/v6/v7 | P1 |

### 3.2 Tier 3 — 信号处理（5 个）

| ID | task_type | 文件 | PAXG 对应 | 优先级 |
|----|-----------|------|----------|--------|
| P7-09 | `spectral_analysis` | `handlers/signal/spectral.py` | v7-E1 | P1 |
| P7-10 | `wavelet` | `handlers/signal/wavelet.py` | v7-W1/W2/W3 | P1 |
| P7-11 | `spectral_entropy` | `handlers/signal/spectral_entropy.py` | v7-E2 | P1 |
| P7-12 | `cross_spectrum` | `handlers/signal/cross_spectrum.py` | v7-E4 | P2 |
| P7-13 | `filter_design` | `handlers/signal/filter.py` | — | P2 |

### 3.3 Tier 4 — 非线性动力学（7 个）

| ID | task_type | 文件 | PAXG 对应 | 优先级 |
|----|-----------|------|----------|--------|
| P7-14 | `mutual_information` | `handlers/nonlinear/mutual_info.py` | v7-N1 | P1 |
| P7-15 | `transfer_entropy` | `handlers/nonlinear/transfer_entropy.py` | **v7-N2 关键** | P1 |
| P7-16 | `hurst_exponent` | `handlers/nonlinear/hurst.py` | v7-N3 | P1 |
| P7-17 | `sample_entropy` | `handlers/nonlinear/sample_entropy.py` | v7-N4 | P1 |
| P7-18 | `permutation_entropy` | `handlers/nonlinear/permutation_entropy.py` | v7-N4 | P1 |
| P7-19 | `rqa` | `handlers/nonlinear/rqa.py` | v7-N5 | P2 |
| P7-20 | `recurrence_plot` | `handlers/nonlinear/recurrence_plot.py` | v7-N5 | P2 |

### 3.4 Tier 5 — 灰色系统（3 个）

| ID | task_type | 文件 | PAXG 对应 | 优先级 |
|----|-----------|------|----------|--------|
| P7-21 | `grey_relation` | `handlers/grey/grey_relation.py` | v7-G1/G2 | P1 |
| P7-22 | `gm11_predict` | `handlers/grey/gm11.py` | v7-G3 | P1 |
| P7-23 | `grey_cluster` | `handlers/grey/grey_cluster.py` | v7-G1 | P2 |

### 3.5 Tier 6 — 机器学习（7 个）

| ID | task_type | 文件 | PAXG 对应 | 优先级 |
|----|-----------|------|----------|--------|
| P7-24 | `ml_train` | `handlers/ml/train.py` | v7-F3 | P1 |
| P7-25 | `ml_predict` | `handlers/ml/predict.py` | v7-F3 | P1 |
| P7-26 | `feature_importance` | `handlers/ml/feature_importance.py` | v7-F3 | P1 |
| P7-27 | `walkforward_cv` | `handlers/ml/walkforward_cv.py` | v7-F3 | P1 |
| P7-28 | `clustering` | `handlers/ml/clustering.py` | v7-E3 | P2 |
| P7-29 | `dimension_reduction` | `handlers/ml/dim_reduction.py` | v7-E3 | P2 |
| P7-30 | `classification_metrics` | `handlers/ml/classification_metrics.py` | v7-F4 | P2 |

---

## 4. 测试计划

| 文件 | 测试数 | 覆盖 |
|---------|--------|------|
| `test_handlers_stats.py` | 50 | 8 个统计 handler |
| `test_handlers_signal.py` | 40 | 5 个信号 handler |
| `test_handlers_nonlinear.py` | 50 | 7 个非线性 handler |
| `test_handlers_grey.py` | 25 | 3 个灰色 handler |
| `test_handlers_ml.py` | 35 | 7 个 ML handler |
| **合计** | **200** | |

---

## 5. 验收标准

| 标准 | 验证方法 |
|------|---------|
| 30 个 handler 全部注册 | `HANDLERS` 含 36 个 task_type（6 Tier1 + 30 高级） |
| 200 项测试全部通过 | `pytest tests/handlers_advanced/ -v` |
| **PAXG v7 N2 传递熵可用** | `transfer_entropy` handler 返回 te_forward/p_value |
| 自实现算法验证 | 灰色关联/GM(1,1)/Hurst/SampEn 与专业库对比 |
| 可选依赖优雅降级 | PyWavelets/nolds 未安装时 fallback 自实现 |

---

## 6. 关键实现要点

### 6.1 传递熵（PAXG v7 N2 关键）

```python
@register("transfer_entropy")
def handle_transfer_entropy(spec, data, *, on_progress=None):
    # 自实现分箱估计器（~60 行 numpy）
    # 含置换检验（100 次）
    # 返回 te_forward / te_backward / net_te / p_value
```

### 6.2 小波分析（PAXG v7 W）

```python
@register("wavelet")
def handle_wavelet(spec, data, *, on_progress=None):
    try:
        import pywt
        # 用 PyWavelets
    except ImportError:
        # fallback 自实现 Morlet CWT
```

### 6.3 自实现验证

| 模块 | 自实现 | 验证基准 |
|------|--------|---------|
| 灰色关联 | ~15 行 | 手工验证 3 样本 |
| GM(1,1) | ~20 行 | 指数增长序列误差 < 1% |
| 传递熵 | ~60 行 | 独立序列 TE ≈ 0 |
| Hurst DFA | ~40 行 | 白噪声 H ≈ 0.5 |
| SampEn | ~20 行 | antropy 对比 |
| RQA | ~80 行 | 视觉验证递归图 |

---

*P7 完成后，V3.1 具备 PAXG v7 全部计算能力，可复现 v1~v7 全部研究。*
