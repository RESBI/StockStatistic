"""StockStat v2.0 — Internal core layer (Layer 0).

This package is an **internal implementation detail**. Public API stability
is not guaranteed. All user-facing APIs remain at the top-level
``stockstat`` package (the compatibility layer).
"""
